#news
    md-content(layout="row")
      md-card#news-card(flex="grow")
        md-toolbar
            .md-toolbar-tools
                //md-button.md-icon-button(aria-label="Back", ui-sref="main")
                //    md-icon(md-svg-icon="/media/images/icons/back.svg")
                span.md-headline Log in

        md-content(layout-padding)
            md-toolbar.md-warn(ng-show="authFailed") Could not log in with given username and password
            md-toolbar.md-accent(ng-show="loggedOut") You have just logged out.
            div(layout="column")
                md-input-container
                    label Email Address
                    input(required, md-no-asterisk, name="username", ng-model="login.username")
                md-input-container
                    label Password
                    input(required, type="password", md-no-asterisk, name="username", ng-model="login.password")

                div
                    md-button.md-primary(md-no-ink, ng-click="login()", ng-disabled="loggingIn") Log In
                    md-progress-circular(md-mode="indeterminate" ng-show="loggingIn")
            
            div(layout="column")
                p If you don't have an accouunt you can create one 
                    a(ui-sref="main.register",ui-sref-opts="{reload: true, notify: true}", ui-sref-active="active") here